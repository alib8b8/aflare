name: Simple Test Workflow
description: Test the workflow execution engine with simple nodes

steps:
  - node: test_node
    params:
      message: "Hello from step 1!"

  - node: test_node
    params:
      message: "Hello from step 2!"
