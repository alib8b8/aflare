{
  "id": "openclaw-aflare",
  "name": "aflare Workflow Integration",
  "version": "1.1.0",
  "description": "Invoke aflare workflows as tools in OpenClaw conversations",
  "main": "./dist/index.js",
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "workflowDir": {
        "type": "string",
        "description": "Directory containing aflare workflow YAML files (default: ./workflows)",
        "default": "./workflows"
      },
      "aflarePath": {
        "type": "string",
        "description": "Path to aflare binary (default: aflare)",
        "default": "aflare"
      },
      "enableAutoDiscovery": {
        "type": "boolean",
        "description": "Auto-discover workflows in the configured directory",
        "default": true
      }
    }
  },
  "permissions": ["network", "exec"]
}
