# Git mailmap — canonical identity display for alib8b8/aflare
#
# Unifies how historical author names/emails are DISPLAYED (git log/shortlog
# honor this); it does not rewrite history. Due-diligence readers should pair
# this file with PROVENANCE.md, which declares which identities belong to the
# project owner.
#
# Canonical identity (GitHub-attributed noreply form, ID-prefixed):
alib8b8 <115916856+alib8b8@users.noreply.github.com>

# --- Confirmed: same GitHub account, two email spellings ---
alib8b8 <alib8b8@users.noreply.github.com> <alib8b8@users.noreply.github.com>

# --- Pending owner confirmation (see PROVENANCE.md §2 before enabling) ---
# Uncomment each mapping only after the owner confirms the identity is theirs:
# alib8b8 <115916856+alib8b8@users.noreply.github.com> Dev <dev@example.com>
# alib8b8 <115916856+alib8b8@users.noreply.github.com> HKAIC User <user@hkaic.example.com>
# alib8b8 <115916856+alib8b8@users.noreply.github.com> llm-box dev <dev@llm-box.local>
# alib8b8 <115916856+alib8b8@users.noreply.github.com> llm-box <aln48xF1OWfWMFC8.llm-box@noreply.gitcode.com>
# alib8b8 <115916856+alib8b8@users.noreply.github.com> Security Audit Bot <security@llm-box.local>

# --- Intentionally NOT mapped ---
# webbrain-one — third-party contributor (see PROVENANCE.md §3)
# dependabot[bot], github-actions — GitHub bots
